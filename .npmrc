# Keep pnpm's default non-hoisted layout for monorepo hygiene.
# Individual packages that @vercel/node's nft can't trace via symlink
# are dereferenced in apps/api/vercel.json installCommand instead.
