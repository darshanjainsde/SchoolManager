{
  "name": "@skoolos/types",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json || true"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
