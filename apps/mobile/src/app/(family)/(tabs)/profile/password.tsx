import { Screen } from '@/components/ui';
import { ChangePasswordCard } from '@/components/ChangePasswordCard';

/** The family twin of the staff Change-password door — see that file. */
export default function FamilyPassword() {
  return (
    <Screen>
      <ChangePasswordCard />
    </Screen>
  );
}
