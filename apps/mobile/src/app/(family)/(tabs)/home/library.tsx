import { LibraryShelf } from '@/components/LibraryShelf';

/** The child's shelf — one component, shared with the teacher's tab. */
export default function FamilyLibrary() {
  return <LibraryShelf />;
}
