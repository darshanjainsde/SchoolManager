{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "baseUrl": ".",
    "noEmit": true,
    "paths": {
      "@skoolos/db": ["../../packages/db/src"],
      "@skoolos/types": ["../../packages/types/src"],
      "@skoolos/config": ["../../packages/config/src"]
    }
  },
  "//": "Workspace packages are listed literally, never as packages/*. The wildcard swept in EVERY package in the monorepo, including ones this app does not import — and a sibling package whose generated Prisma client is gitignored (and generated by its own install step, not this app's) then failed `ncc build` with TS2307 on a file apps/api never references. Adding a package to the monorepo must not be able to break this app's deploy.",
  "include": [
    "src/**/*",
    "server.ts",
    "../../packages/config/src/**/*",
    "../../packages/db/src/**/*",
    "../../packages/types/src/**/*"
  ],
  "ts-node": {
    "transpileOnly": true,
    "compilerOptions": {
      "module": "commonjs",
      "noEmit": false
    },
    "require": ["tsconfig-paths/register"]
  }
}
