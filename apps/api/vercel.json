{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd ../.. && NODE_ENV=development pnpm install --frozen-lockfile=false && pnpm --filter @skoolos/db generate && node apps/api/scripts/deref-pnpm-symlinks.mjs",
  "buildCommand": "pnpm run bundle",
  "functions": {
    "api/index.js": {
      "maxDuration": 60,
      "includeFiles": "../libquery_engine-rhel-openssl-3.0.x.so.node"
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.js"
    }
  ]
}
