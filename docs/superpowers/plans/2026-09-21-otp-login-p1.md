# Plan — PR 1: OTP login, profile switching, admin profile (staging)

Spec: `docs/superpowers/specs/2026-09-21-whatsapp-identity-and-actions-design.md`.
Branch: `feat/otp-login` off `feat/whatsapp-actions` (PR #93). Target: staging.

Each task ends green (`jest`/`vitest` for the files it touches); the PR ends
with `pnpm preflight`. Tests are named after the edge cases in spec §6.

## T1 — Schema
- `User.name String?`, `User.notifyPrefs Json?`
- `Student.guardianPhoneE164 String?` + index `[schoolId, guardianPhoneE164]` and `[guardianPhoneE164]`
- `Teacher.phoneE164`, `Staff.phoneE164` + indexes
- `OtpChallenge` (platform table; allow-listed in `packages/db/src/rls-coverage.spec.ts`)
- migration `20260921_000000_otp_login` with a SQL back-fill of the E.164
  columns (Indian default: last 10 digits starting 6–9 → `+91…`)
- `packages/db` tests green

## T2 — OTP engine (`apps/api/src/common/otp/`)
- `phone-identity.ts`: `toE164` re-export + `maskPhone`
- `otp.service.ts`: start / check / limits; `otp-senders.ts`: `OtpSender`,
  `WhatsAppOtpSender`, `SmsOtpSender` (disabled without env); `OtpModule`
- ErrorCodes: `OTP_RATE_LIMITED`, `OTP_UNDELIVERABLE`, `OTP_EXPIRED`,
  `OTP_WRONG`, `OTP_LOCKED`, `OTP_CHALLENGE_UNKNOWN`, `PROFILE_NOT_SWITCHABLE`
- spec: limits, fan-out records `sentVia`, no sender → undeliverable, check
  semantics (wrong/expired/locked/consumed)
- `PhoneVerifyService` sends through the fan-out

## T3 — Phone profiles (`apps/api/src/modules/auth/internal/phone-profiles.service.ts`)
- `resolve(phoneE164, { schoolId? })` per spec §2, host from the school's
  primary LIVE domain or `<slug>.<PLATFORM_HOST>`
- `sameIdentity(userId)` → the E.164 set a login owns (for switch/profiles)
- spec: the six identity edge cases

## T4 — Auth routes
- `otp/request`, `otp/verify`, `otp/choose`, `profiles`, `switch`,
  `reset-with-otp`; `forgot-password` also starts a RESET challenge
- `AuthService.issueFor(userId)` public seam (wraps `issueTokens`)
- DTOs + spec for each route's refusals

## T5 — Keep E.164 columns true
- students / teachers / staff create+update write the E.164 beside the raw
  phone; import paths too (grep every `guardianPhone:` / `phone:` write)
- spec: a write with "98765 43210" stores `+919876543210`

## T6 — `/me/profile` + admin profile page (web)
- API: `GET/PATCH /me/profile` (name, phone, notifyPrefs) for admin/teacher/staff
- Web: `/app/profile` page (name, email, password card, PhoneCard moved here,
  notification switches); avatar door in the console header; "My profile"
  nav item; Settings loses PhoneCard
- tests: page renders three states; nav-model test updated

## T7 — Web login + switch
- Gatehouse: Phone tab (default) → code → chooser (same host) → home
- Forgot: "code on WhatsApp" path → reset-with-otp
- Switch profile menu in portal / teacher / console headers
- tests: phone happy path, chooser, wrong code, switch menu hidden with one profile

## T8 — App login + shelf
- `(auth)/login.tsx`: phone-first with code step; chooser sheet
- `family-store.ts` → generic profiles (kind/role/label/host/session);
  migration of the persisted shelf key
- Switch sheet on every role's Profile tab; cross-school = new slot
- tests: shelf generalisation keeps sibling tests green; staff switch

## T9 — Ship to staging
- `pnpm preflight`; PR (base `feat/whatsapp-actions` until #93 merges)
- migration on staging from the branch ref; push staging; smoke:
  `/auth/otp/request` on raffles with the test recipient; chooser with a
  teacher-who-is-parent fixture on the Raffles sample school
- memory + templates.md updated
